#include <stdio.h>
#include <stdlib.h>

int main()
{ int a,b;
    printf("enter a no \n");
    scanf("%d",&a);
    printf("enter second no.");
    scanf("%d",&b);
    if (a==b)
    {
        printf("the no. are equal ");

    }
    else
        printf("the no. entered are not equal");

    return 0;
}
